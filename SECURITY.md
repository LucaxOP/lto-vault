# Security

Do not commit production workbooks, server paths, credentials, or generated user configuration.

The application processes workbooks locally. It does not upload workbook contents. Report security issues privately to the repository maintainer instead of opening a public issue containing sensitive data.
